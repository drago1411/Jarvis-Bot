{
  "name": "jarvis-workbench",
  "version": "1.0.0",
  "description": "JARVIS workbench — all websites built by JARVIS live here",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "new": "node scripts/new-project.js"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
